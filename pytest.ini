[pytest]
addopts = -p no:cacheprovider -m "not network and not model and not gpu"
testpaths = tests
markers =
    network: 需要访问外部数据源或网络服务。
    model: 需要加载真实 Kronos 模型，可能使用本地模型缓存或网络。
    gpu: 需要 CUDA 或其他 GPU 环境。
