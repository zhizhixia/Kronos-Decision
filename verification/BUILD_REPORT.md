# 交接包制作核验记录

- 制作时间（UTC）：2026-09-17T04:27:29.699101+00:00
- 源码基线与仓库：`89c62ed7e64f338882bf58654bcce39086475a30`，<https://github.com/zhizhixia/Kronos-Decision.git>
- 实施状态：尚未开始。此文件记录的是打包与校验，不是项目测试通过。

## 已执行的只读核验

| 项 | 结果 |
| --- | --- |
| 源码 clone 与 checkout 到固定基线 | 通过，工作区无差异 |
| `git fsck --full` | 通过 |
| 离线 bundle 创建与 `bundle verify` | 通过 |
| 从 bundle 到空目录离线恢复并逐字节比对 129 个文件 | 通过，0 个不一致 |
| 两份计划、enough 规则与源文件字节一致 | 通过 |
| 校验脚本 8 项成功/失败路径 | 全部通过（正常/重复/越界/错哈希/版本/被改/丢失/丢清单） |
| 高置信凭据模式扫描（当前跟踪文件） | 0 命中 |
| 活动 Git hook、LFS 指针、符号链接 | 无 |

## 范围与局限

- SHA-256 清单用于发现复制损坏和文件变化，不是数字签名，不能抵御清单与文件同时被替换。
- 源码未被修改；校验脚本不会运行项目代码、安装依赖或访问网络。
- 未扫描完整 Git 历史中的敏感内容，也未审计仓库全部业务实现。
- 原仓库 `AGENTS.md` 与本次协作方式的冲突未改动，仅记录于 `handoff/REVIEW_NOTES.md`。
- 模型权重、市场数据、个人运行数据和 Python 环境没有随包迁移。

## 使用顺序

1. 复制/解压到新目录，运行 `python scripts/verify_handoff.py`。
2. `git -C source/Kronos-Decision fsck --full` 与 `status --short`、`rev-parse HEAD`。
3. 阅读 `handoff/` 与两份 `plans/`，完成只读 PRE0。
4. 仅在取得 M0 实施授权后，从 SA01 开始；不要运行旧启动脚本或直接跑全量测试。


## 跨电脑只读审阅结论（已在打包中修复）

独立只读审阅（扮演无旧会话的新接手者）发现两项并已修复：

1. 离线恢复到 `restored-source/` 后，项目根目录与接手路径原先仍指向 `source/Kronos-Decision/`；已在 README 中明确恢复后切换到 `restored-source/`，原目录只作对照。
2. 执行计划的上位计划原写 `.hermes/plans/`，在交接包中实际位于根目录 `plans/`；已改为“本文件同目录 + 目录映射说明”。

以上仅涉及交接说明路径一致性，不改变源码、计划任务划分或验收标准。
